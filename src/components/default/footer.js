import React from 'react';
import {Layout} from 'antd';

import './footer.css'

const {Footer} = Layout;

export default () => {
    return (
        <Footer className='footer'>
            Footer
        </Footer>
    );
}